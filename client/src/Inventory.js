import React, {Component} from 'react';

class Inventory extends Component {
    render(){
        return(
            <div>
                here's the History Items list
            </div>
        )
    }
}

export default Inventory;